import { StyleSheet } from 'react-native';


const image = {
  height: 50,
  width: 50,
  margin: 0,
  padding: 0,
};

const style = StyleSheet.create({
  image: { ...image },

});

export default style;
